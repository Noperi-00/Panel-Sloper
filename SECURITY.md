# Security Policy

برای اجرای عمومی Panel Sloper، مقدارهای `ADMIN_PASSWORD` و `SECRET_KEY` را فقط در Variables امن Railway یا secret manager قرار دهید و هرگز در repository commit نکنید.

رمز اولیهٔ توسعه `Noperi` و نام کاربری اولیه `Noperi` فقط برای راه‌اندازی اولیه هستند. پس از اولین ورود، از بخش Settings مشخصات ورود را تغییر دهید. در صورت مشاهدهٔ مشکل امنیتی، اطلاعات حساس را در issue عمومی منتشر نکنید.
